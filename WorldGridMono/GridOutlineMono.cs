namespace WorldGridMono
{
    using SharedTypes;
    using System;
    using UnityEngine;
    using WorldGridSharedTypes;

    public static class GridOutlineMono
    {
        public static GameObject Create(OutlineBlockType blockType, BlockRotationType rotation, SettingType setting)
        {
        }

        public static GameObject Create(OutlineBlockType blockType, BlockRotationType rotation, float xRotation, float zRotation, SettingType setting)
        {
        }

        private static GameObject Create(OutlineBlockType blockType, BlockRotationType rotation, bool rotateXZ, float xAngle, float zAngle, SettingType setting)
        {
        }
    }
}

