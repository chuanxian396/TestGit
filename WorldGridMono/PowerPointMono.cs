namespace WorldGridMono
{
    using SharedTypes;
    using System;
    using UnityEngine;
    using WorldGridSharedTypes;

    public static class PowerPointMono
    {
        public static GameObject Create(PowerPointState ppState, TerrainType terrain)
        {
        }

        private static GameObject GetPrefab(PowerPointState state, TerrainType terrain)
        {
        }

        private static GameObject GetPrefabByTerrain(TerrainType terrain)
        {
        }
    }
}

